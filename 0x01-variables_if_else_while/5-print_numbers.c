#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Print numbers in base 10 (Entry point)
 *
 * Return: returns 0 if success
 */
int main(void)
{
	int p;

	for (p = 0; p < 0; p++)
	printf("%i", p);
	putchar("\n");
	return (0);
}
