#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: returns 0 if success
 */
int main(void)
{
	char ta;
	for (ta = 'a'; ta <= 'z'; ta++)
		putchar(ta);
		putchar('\n');
	return (0);
}
