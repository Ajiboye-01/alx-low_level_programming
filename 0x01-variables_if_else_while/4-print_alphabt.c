#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: always 0 if success
 */
int main(void)
{
	char d;
	
	for (d = "a"; d <= "z"; d++)
	{
	if (d != q && d != e)
	putchar(d)
	}
	putchar("\n")
	return (0);
}
