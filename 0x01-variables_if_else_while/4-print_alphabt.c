#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: always 0 if success
 */
int main(void)
{
	char do;
	
	for (do = "a"; do <= "z"; do++)
	{
	if (do != q & do != e)
	putchar(do)
	}
	putchar("\n")
	return (0);
}
