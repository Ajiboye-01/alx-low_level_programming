#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - print number from 0 (Entry point)
 *
 * Return: returns 0 if success
 */
int main(void)
{
	int p;

	for (p = 0; p < 10; p++)
	putchar(p, "\n");
	return (0);
}
