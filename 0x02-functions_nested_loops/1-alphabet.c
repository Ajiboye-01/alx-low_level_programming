#include "main.h"
/**
 * main - Entry point
 *
 * Return: returns 0 if success
 */
void print_alphabet(void)
{
	char ta;

	for (ta = 'a'; ta <= 'z'; ta++)
	putchar(ta);
	putchar('\n');
	return (0);
}
